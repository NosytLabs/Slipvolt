# Reliability and cleanup maintenance

Existing user flow, token rules, planned prices, native-GNK budgets and manual-only Actions remain unchanged. Work is isolated in a local source copy. No provider credential, signing key or live paid call is needed.

1. Reproduce and fix the network cache's stale-to-fresh regression, use monotonic expiry, reject impossible dates, and expire old failed snapshots.
2. Bound and amortize security-table housekeeping; preserve exact rate admission; index wallet session removal; roll back transactions on cancellation/interrupt.
3. Reproduce and fix fragmented SSE handling and stop reading at the terminal marker, without automatic paid retries.
4. Cancel invalidated preflight checks and prevent stale success styling/results after edits, failures or account changes.
5. Run all Python/JS/browser checks including preserved help/privacy/terms; package a full source tree, cumulative patch and change-only patch with test evidence.

Reference baseline Git tree: 71e9d7ee60bf4e059e26be9f0250296edca7df1b. Remote commit observed: fe1d1467fda6a5500f8411f031ef9601fde25a38. Applying the previous pending patch preserves the prior setup doctor, credential isolation, status page and preflight work.
